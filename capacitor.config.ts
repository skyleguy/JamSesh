import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.base',
  appName: 'Ionic Base',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
